---
export interface Props {
	text: string;
	href: string;
}

const { text, href } = Astro.props;
---

<a class='btn text-gradient' href={href}>{text}</a>

<style scoped>
	.btn {
		position: relative;
	}
</style>
